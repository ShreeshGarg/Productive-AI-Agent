version: '3.8'

services:
  productivity-agent:
    build: .
    ports:
      - "5000:5000"
    environment:
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
      - AGENT_MODE=production
    restart: unless-stopped
